# 2026-08-03

- Brutal's self-hosted integration is pinned at annotated tag
  `brutal-integration-v0.1.0`, commit
  `c1e9be524440cfc9c1328e07fc6e542bf5a4a382`, based on upstream
  `9d19e439905a9a954ccdefe22d9270d7c389695d`.
- The integration requires `AUTH_MODE=cloudflare_access`, one allowlisted
  service-token common name mapped to a fixed internal user/organization, a
  custom Access hostname, and disabled direct `workers.dev`/preview access.
- Service audits require exactly 50 pages and explicit Lighthouse; limits are
  ten pages per device, two global/one-per-origin audits, USD 0.25 per audit,
  USD 10 per UTC day, USD 100 per month, and seven-day raw retention.
- Current local release verification: 796 tests, zero lint warnings/errors,
  typecheck, format check, build, and clean D1 migration replay through `0037`
  pass at the tagged commit.
- Live Access restart/rotation, 24-hour unattended operation, staging contract
  parity, direct-origin and external rebinding probes, DataForSEO cost, and
  retention capacity evidence remain production blockers.
- Review hardening is released as `brutal-integration-v0.1.1`; its reviewed
  integration code is `e151f16ef6d5e8333069b7cd093e2cfa6a1876ba`.
  It adds bounded DNS validation, IPv4-compatible IPv6 rejection,
  cross-origin credential stripping, explicit ambiguous-dispatch recovery,
  retry-safe Lighthouse result reuse and spend accounting, independent
  scheduled cleanup failures, and 1,000-key R2 deletion batches.
- The updated local release gate passes 808 tests, typecheck, lint, formatting,
  the full client/SSR build, and a clean D1 replay through migration `0037`.
- `brutal-integration-v0.1.2` supersedes `v0.1.1` with reviewed integration
  code `dda2ea60eb834686e172e29b0151f4afabc7cf9d`. Every audit MCP output is
  strict, and the `tools/list` fixture now detects added, removed, or renamed
  audit tools without depending on registration order. The same
  808-test/typecheck/lint/format/build gate passes for this revision.
